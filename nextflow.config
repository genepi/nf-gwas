// default: resume
resume = true
process.container= 'quay.io/genepi/gwas-regenie:v0.1.0'
// to pull from github
manifest.mainScript = 'gwas-regenie.nf'

profiles {
    debug { process.beforeScript = 'echo $HOSTNAME' }

    standard {
        docker.enabled         = true
        docker.userEmulation   = true
        singularity.enabled    = false
    }
    singularity {
        singularity.enabled    = true
        singularity.autoMounts = true
        docker.enabled         = false
    }

    manifest {
    name            = 'gwas-regenie'
    author          = 'Lukas Forer, Sebastian Schönherr'
    homePage        = 'https://github.com/lukfor/gwas-regenie'
    description     = 'Nextflow GWAS Pipeline.'
    mainScript      = 'main.nf'
    nextflowVersion = '!>=21.04.0'
    version         = '0.1'
}

}
